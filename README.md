# DevTrack

Now serving... placeholder.

> Full documentation is written at the end of the build (see final README).

Status: **under construction**.

Roadmap: HTML → CSS → JavaScript → Python → Flask → SQLite → REST API → Authentication → Projects → Contribution Graph → Streaks → Analytics → Tests → Release.